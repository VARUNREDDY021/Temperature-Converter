version: 0.2

phases:
  install:
    commands:
      - echo "No dependencies to install for a standalone JavaScript project."
  build:
    commands:
      - echo "Running custom scripts or preparing files..."
      # Add any required build or processing commands here, if applicable
artifacts:
  files:
    - '**/*.js'   # Include all JavaScript files
    - '**/*.html' # Include HTML files if needed
    - '**/*.css'  # Include CSS files if applicable
